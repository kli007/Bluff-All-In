extends Node


# Called when the node enters the scene tree for the first time.
#func _ready() -> void:
	#$Player.defineNodeVariables() # defining the dictionary for nodes in player once all scenes are loaded
